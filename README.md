
  # MeMori (Good Version)

  This is a code bundle for MeMori (Good Version). The original project is available at https://www.figma.com/design/R3IkGJ4utsTsVF3oB3ac5B/MeMori--Good-Version-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  